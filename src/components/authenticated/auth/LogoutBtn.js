import React from 'react'

const LogoutBtn = () => {
  return (
    <button onClick={() => console.log('click from button in LogoutBtn.js')}>
      Log Out
    </button>
  )
}

export default LogoutBtn
