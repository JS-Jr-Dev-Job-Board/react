import theme from './index'

const darkTheme = {
  ...theme,
  color: {
    background: 'black',
    primaryColor: '#FF6347',
    secondaryColor: 'yellow',
    tertiaryColor: 'blue'
  }
}

export default darkTheme
