import theme from './index'

const lightTheme = {
  ...theme,
  // None of these are set colors
  color: {
    background: 'white',
    secondaryBackground: '#eee',
    primary: '#FF6347',
    secondary: 'brown',
    tertiary: 'red',
    accent: 'green'
  }
}

export default lightTheme
