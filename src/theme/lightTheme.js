import theme from './index'

const lightTheme = {
  ...theme,
  color:{
    background: 'white',
    primaryColor: '#FF6347',
    secondaryColor: 'brown',
    tertiaryColor: 'red'
  }
}

export default lightTheme
